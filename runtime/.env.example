OPENAI_API_KEY=
OPENAI_MODEL=gpt-6-astra
PORT=4317
# Local report storage; ignored by Git
DATA_DIR=./data
# Only set this when using that provider's own API key.
OPENAI_BASE_URL=https://api.openai.com/v1
